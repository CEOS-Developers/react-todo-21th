import styled from 'styled-components';

import calendarIcon from '@/assets/calendar.svg?react';

export const CalendarIcon = styled(calendarIcon)`
  width: clamp(2.8rem, 2.2vw, 4.2rem);
  height: fit-content;
`;
