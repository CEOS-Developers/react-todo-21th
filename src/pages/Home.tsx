import Calendar from '@/components/Calendar/Calendar';

import * as S from './Home.styled';

const Home = () => {
  return (
    <S.HomeContainer>
      <Calendar />
    </S.HomeContainer>
  );
};

export default Home;
